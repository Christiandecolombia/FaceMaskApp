import React from "react";
import { View, Text } from "react-native";

const Landing = () => {
    return (
        <View
            style={{ flex: 1, alignItems: "center", justifyContent: "center" }}
        >
            <Text> Welcome to the shits</Text>
        </View>
    );
};

export default Landing;
